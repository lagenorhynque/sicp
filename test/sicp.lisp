(defpackage sicp-test
  (:use :cl
        :sicp
        :prove))
(in-package :sicp-test)

;; NOTE: To run this test file, execute `(asdf:test-system :sicp)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
